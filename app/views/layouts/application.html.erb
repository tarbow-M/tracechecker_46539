<!DOCTYPE html>
<html class="h-full bg-gray-50">
  <head>
    <title>TraceChecker</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload", defer: true %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
  </head>

  <body class="h-full flex flex-col">
    
    <!-- ▼▼▼ ヘッダーパーシャルを読み込む ▼▼▼ -->
    <%= render 'layouts/header' %>


    <main class="flex-grow">
      
      <!-- フラッシュメッセージ (ログイン成功・失敗など) -->
      <div class="w-full max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <% if notice %>
          <div class="my-4 p-3 rounded-lg bg-green-100 text-green-700 border border-green-200" role="alert">
            <%= notice %>
          </div>
        <% end %>
        <% if alert %>
          <div class="my-4 p-3 rounded-lg bg-red-100 text-red-700 border border-red-200" role="alert">
            <%= alert %>
          </div>
        <% end %>
      </div>

      <!-- 各ページのビュー (index.html.erb など)挿入 -->
      <%= yield %>

    </main>

    <!-- ▼▼▼ フッターパーシャルを読み込む ▼▼▼ -->
    <%= render 'layouts/footer' %>

  </body>
</html>

